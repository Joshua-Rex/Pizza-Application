All the front end information is within the App.js located in client/src/App.js.
All the back end information is stored within the index.js located in container/server/index.js.

To run, a terminal needs to call npm start from the client file to run the app.js and a terminal needs to call npm start from the container file for index.js.
The website will then be hosted on localhost:3000.